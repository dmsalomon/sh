- set builtin (for args)
- $@
- variable substitution extensions
  - parameterize expansion using a struct instead of globals
- sourcing /etc/profile and .profile
- implement case statement
- aliases
